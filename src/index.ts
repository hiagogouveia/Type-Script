let age: number = 5
const firtsName: string = 'Hiago'
const isValis: boolean = true

age = 5

console.log(age);

//lista de tupla
const people: [number, string][] = [
    [1, 'joe'],
    [2, 'samuel']
];

//Intercections
const prodctId: string | number | boolean = false


// Enum
enum Direction {
    Up = 1,
    Down = 2
}

const direction = Direction.Up